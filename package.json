{
  "name": "prometheus",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc -b && node dist/index.js",
    "server": "npx esbuild ./src --bundle --platform=node --outfile=dist/index.js && node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/express": "^5.0.0",
    "esbuild": "^0.24.2",
    "express": "^4.21.2",
    "prom-client": "^15.1.3",
    "prometheus": "file:"
  }
}
